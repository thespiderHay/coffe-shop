# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty scaffolding for a coffee shop website (part 1 of a build-and-deploy project). The only existing content is a placeholder directory tree:

```
docs/design/references/
```

No source code, package manifest, framework, or build tooling has been set up yet, and this directory is not yet a git repository.

## Working here

- There are no build/lint/test commands to run yet — none of that tooling exists in the repo. Once a stack is chosen and scaffolded, update this file with the actual commands (dev server, build, lint, test — including how to run a single test).
- Once real architecture exists (frontend framework, styling approach, deployment target, etc.), replace this section with the actual high-level structure — how pages/components are organized, where content/data lives, and how the site is deployed.
- `docs/design/references/` is intended to hold design reference material for the site; check there for visual/UX direction before making design decisions.
